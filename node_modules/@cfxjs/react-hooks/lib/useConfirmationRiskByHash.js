"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useConfirmationRiskByHash;

var _ = require("./");

const GET_CONFIRMATION_RISK_SWR_ID = "GET_CONFIRMATION_RISK_SWR_ID";

function useConfirmationRiskByHash(blockHash) {
  const {
    data: confirmationRisk,
    error: getConfirmationRiskError
  } = (0, _.useEpochNumberSWR)(blockHash ? [GET_CONFIRMATION_RISK_SWR_ID, blockHash] : null, () => {
    var _window, _window$confluxJS;

    return (_window = window) === null || _window === void 0 ? void 0 : (_window$confluxJS = _window.confluxJS) === null || _window$confluxJS === void 0 ? void 0 : _window$confluxJS.getConfirmationRiskByHash(blockHash);
  });
  if (getConfirmationRiskError) console.error(`Error get confirmationRisk: ${getConfirmationRiskError.message}`);
  return confirmationRisk;
}
//# sourceMappingURL=useConfirmationRiskByHash.js.map